# ============================================================
# Fact2PDF - Environment Variables
# Copier ce fichier en .env et remplir les valeurs
# NE JAMAIS committer le fichier .env
# ============================================================

# Application
APP_NAME="Fact2PDF"
APP_ENV=development          # development | production
APP_URL=http://localhost:8080
APP_SECRET=change_this_secret_key_min_32_chars

# Sécurité production (voir README_PRODUCTION_DEPLOYMENT.md)
# FORCE_HTTPS=1               # Redirection HTTPS (1 en prod derrière reverse proxy)
# SESSION_SECURE=1            # Cookie session en Secure uniquement (1 en prod)
# CORS_ORIGINS=https://votre-app.com  # Origine(s) autorisée(s) pour l’API (vide = *)

# Base de données
DB_HOST=db                   # 'db' pour Docker, 'localhost' sinon
DB_PORT=3306
DB_NAME=fact2pdf
DB_USER=fact2pdf_user
DB_PASS=change_this_password
DB_CHARSET=utf8mb4

# JWT (API Mobile)
JWT_SECRET=change_this_jwt_secret_min_32_chars
JWT_EXPIRY=3600              # secondes (1h)

# Upload
UPLOAD_MAX_SIZE=5242880      # 5MB en bytes
# Doit être sous public/ pour que /storage/uploads/ soit accessible par le navigateur
UPLOAD_PATH=/var/www/html/public/storage/uploads

# Email (PHPMailer)
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USER=noreply@example.com
MAIL_PASS=mail_password
MAIL_FROM_NAME="Fact2PDF"

# Session (optionnel)
# SESSION_LIFETIME=3600
# SESSION_NAME=fact2pdf_sess

# Logs
LOG_LEVEL=debug              # debug | info | warning | error
LOG_PATH=/var/www/html/storage/logs
